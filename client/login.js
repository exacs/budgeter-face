/**
 * File to be served in "GET /login"
 */
import React from 'react';
import ReactDOM from 'react-dom';

import LoginForm from './scenes/login-form';

ReactDOM.render(<LoginForm />, document.getElementById('root'));
