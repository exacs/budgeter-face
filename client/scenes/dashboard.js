import React from 'react';
import styled from 'styled-components';
import { hot } from 'react-hot-loader';

import CreateBudget from './create-budget';

const App = () => <CreateBudget />;

export default hot(module)(App);
